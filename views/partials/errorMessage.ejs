<!-- RENDER the text from the authors,js-->
<!-- need to wrap in a if statement because it is not defined and will only bring it up if it is defined-->
<% if(locals.errorMessage !=  null) { %> 
    <%= errorMessage %> 
<% } %> 